% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{compareCellChatsM}
\alias{compareCellChatsM}
\title{Compare Multiple CellChat Objects}
\usage{
compareCellChatsM(
  object.list,
  comparison = NULL,
  reference = NULL,
  pathways = NULL,
  measure.type = c("sender", "receiver", "both", "influence"),
  cell.type.strategy = c("shared", "union"),
  thresh = 0.05,
  norm.method = c("none", "z-score", "minmax", "relative"),
  show.pathway = FALSE,
  show.heatmap = FALSE,
  return.data = FALSE,
  label.timeline = FALSE,
  label.pathway = FALSE,
  label.top = 5,
  label.size = 3
)
}
\arguments{
\item{object.list}{List of CellChat objects}

\item{comparison}{Vector of indices to compare (NULL for all objects)}

\item{reference}{Integer or character, index or name of the reference object}

\item{pathways}{Vector of pathway names to include (NULL for all)}

\item{measure.type}{Character, type of measure to calculate: "sender", "receiver", "both", or "influence"}

\item{cell.type.strategy}{Character, strategy for aligning cell types: "shared" (default) or "union"}

\item{thresh}{P-value threshold for significant interactions}

\item{norm.method}{Method for normalizing values: "none", "z-score", "minmax", or "relative"}

\item{show.pathway}{Logical, whether to return pathway contribution plot (default: FALSE)}

\item{show.heatmap}{Logical, whether to return heatmap visualization (default: FALSE)}

\item{return.data}{Logical, whether to return data along with visualizations (default: FALSE)}

\item{label.timeline}{Logical, whether to add labels to the timeline plot (default: FALSE)}

\item{label.pathway}{Logical, whether to add labels to the pathway plot (default: FALSE)}

\item{label.top}{How many top values to label (default: 5, set to Inf for all)}

\item{label.size}{Numeric, size of the labels (default: 3)}
}
\value{
By default, returns the timeline plot. If show.pathway or show.heatmap are TRUE,
  returns a list of plots. If return.data is TRUE, also includes data in the returned list.
}
\description{
This function compares multiple CellChat objects to identify global changes in
communication patterns across conditions.
}
\examples{
\dontrun{
# Load example data
data(cellchatlist)

# Basic global comparison (shared cell types)
result <- compareCellChatsM(cellchatlist)

# Use union strategy to include all cell types
result <- compareCellChatsM(cellchatlist,
                            cell.type.strategy = "union")

# Compare specific measure with union strategy
result <- compareCellChatsM(cellchatlist,
                            measure.type = "sender",
                            cell.type.strategy = "union",
                            show.pathway = TRUE)

# Full comparison with all visualizations
result <- compareCellChatsM(cellchatlist,
                            measure.type = "influence",
                            cell.type.strategy = "union",
                            show.pathway = TRUE,
                            show.heatmap = TRUE,
                            return.data = TRUE)

# With z-score normalization
result <- compareCellChatsM(cellchatlist,
                            cell.type.strategy = "union",
                            norm.method = "z-score",
                            label.timeline = TRUE)
}

}
